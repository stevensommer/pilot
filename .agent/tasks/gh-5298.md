# GH-5298

**Created:** 2026-09-03

## Problem

GitHub Issue GH-5298: fix(autopilot): remove `pilot` label when applying `pilot-superseded` on CI-f...

<!--autopilot-meta
parent: GH-5297
inherited-spec: true
-->

Parent: GH-5297

In `internal/autopilot/controller.go` (`notifyExternalClose()` around line 9327, and any sibling site that applies `pilot-superseded`), extend the label operation to also strip the `pilot` label in the same call so no open issue ever carries both `pilot` and `pilot-superseded`. Add a table-driven test in the autopilot package that drives the CI-fail supersede path and asserts (a) `pilot-superseded` is applied, (b) `pilot` is removed, and (c) the autopilot-fix issue is still created as before. Do not change autopilot-fix spawn semantics or the existing #5273 reaper path.

## Scope fence

Implement ONLY the slice described above. The parent issue GH-5297 is decomposed
into sibling sub-issues that cover the rest of its spec — consult the parent
for context, but do NOT implement parts of it that fall outside this subtask.

## Acceptance Criteria

